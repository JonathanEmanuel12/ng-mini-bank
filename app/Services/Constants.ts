export enum ErrorMessages {
    USER_NOT_CREATED = 'Não foi possível criar sua conta. Tente novamente mais tarde',
    ONLY_ACCOUNT_OWNER = 'Somente o proprietário da conta pode acessá-la'
}

export enum ErrorStatus {
    FORBIDDEN = 403
}

export enum ErrorCode {
    FORBIDDEN = 'FORBIDDEN'
}